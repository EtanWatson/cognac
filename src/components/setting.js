//
import $ from 'jquery'
import React from 'react';
import {render} from 'react-dom';
const Setting = React.createClass({
   render(){
       return(
           <div>系统设置</div>
       )
   }
});
export{Setting}